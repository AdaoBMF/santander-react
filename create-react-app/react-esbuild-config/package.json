{
  "name": "react-esbuild-config",
  "version": "1.0.0",
  "scripts": {
    "start": "esbuild src/index.js --bundle --outfile=dist/bundle.js --loader:.js=jsx --minify --watch --servedir=dist --serve=3000",
    "build": "esbuild src/index.js --bundle --outfile=dist/bundle.js --loader:.js=jsx --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.21.3"
  }
}
